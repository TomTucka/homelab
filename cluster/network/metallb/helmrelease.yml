---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: network
spec:
  chart:
    spec:
      chart: metallb
      version: v0.10.2
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: flux-system
  interval: 1m0s
  releaseName: metallb
  values:
    configInline:
      address-pools:
        - name: default
          protocol: layer2
          addresses:
            - 10.20.40.40-10.20.40.50


